// Vue.config.js
module.exports = {
  // publicPath: '',
  /**
   *  设置项目部署的基础路径
   *  默认不加地址为 http://localhost:8081/
   *  添加publicPath: 'testPath' 后打包地址为 http://localhost:8081/testPath
   *  例如，如果你的应用被部署在 https://www.my-app.com/my-app/，则设置 publicPath 为 /my-app/
   */
  outputDir: 'distAA'
}